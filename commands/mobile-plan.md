---
description: Generate a mobile plan for a direct Expo/React Native build
argument-hint: [optional goal]
---

Run:

!`bose mobile plan`

Then read `.bose/mobile-plan/MOBILE_PLAN.md` and turn it into a step-by-step implementation plan for the current project. If `$ARGUMENTS` is present, use it as the mobile build goal.
